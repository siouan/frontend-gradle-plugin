pluginManagement {
    plugins {
        id 'java'
        id 'war'
        id 'org.siouan.frontend' version '2.0.1'
}

repositories {
        gradlePluginPortal()
    }
}

include 'backend', 'frontend'
