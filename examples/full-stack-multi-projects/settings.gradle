pluginManagement {
    plugins {
        id 'java'
        id 'war'
        id 'org.siouan.frontend' version '2.1.0'
}

repositories {
        gradlePluginPortal()
    }
}

include 'backend', 'frontend'
